package com.drive.roadhazard

object Config {
    /**
     * The single switch to control simulation mode for the entire app.
     * true = Use fake location and sensor data.
     * false = Use the phone's real GPS and sensors.
     */
    const val IS_SIMULATION_MODE = false
}